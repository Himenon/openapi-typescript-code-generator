description: "成功"
headers:
  ResponseTime:
    description: Performance Measurement
    required: true
    allowEmptyValue: false
  Origin:
    $ref: "../../headers/Origin.yml"
content:
  application/json:
    schema:
      type: object
      required:
        - meta
        - reason
      properties:
        meta:
          $ref: "#/components/schemas/ResponseMeta"
        reason:
          type: string
